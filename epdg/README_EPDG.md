# E-PDG Builder (Minimal)

Run:
```
pip install pyyaml   # if using YAML effects
python scripts/build_epdg.py -i data/submissions/A -o data/artifacts/json -e effect_summaries.yaml
```
Outputs `*.epdg.json` per input `.py` file.
